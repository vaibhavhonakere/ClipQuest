services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: clipquest-frontend
    environment:
      VITE_API_BASE_URL: http://localhost:8000
    depends_on:
      ingest-api:
        condition: service_started
    ports:
      - "5173:5173"
